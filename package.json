{
  "name": "autofix-mermaid",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "test": "jest --runInBand",
    "test:ci": "jest --ci --runInBand",
    "build:core": "tsc -p tsconfig.json",
    "smoke:sequence": "node scripts/smoke-sequence.mjs"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.4.0"
  }
}
