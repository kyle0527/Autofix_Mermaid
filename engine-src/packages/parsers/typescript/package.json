{
  "name": "@diagrammender/parsers-typescript",
  "version": "0.3.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "dependencies": {
    "@diagrammender/types": "0.3.0",
    "tree-sitter": "^0.20.0",
    "tree-sitter-typescript": "^0.20.3"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=21.0.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build --silent && node --test dist/__tests__"
  }
}
