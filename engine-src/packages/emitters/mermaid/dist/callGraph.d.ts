import { IRProject, MermaidFragment } from '@diagrammender/types';
export declare function emitCallGraphFragments(ir: IRProject): MermaidFragment[];
export declare function emitCallGraph(ir: IRProject): string;
