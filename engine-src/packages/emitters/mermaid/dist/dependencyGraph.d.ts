import { IRProject, MermaidFragment } from '@diagrammender/types';
export declare function emitDependencyGraphFragments(ir: IRProject): MermaidFragment[];
export declare function emitDependencyGraph(ir: IRProject): string;
