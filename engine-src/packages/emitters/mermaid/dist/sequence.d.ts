import { IRProject, MermaidFragment } from '@diagrammender/types';
export declare function emitSequenceFragments(ir: IRProject): MermaidFragment[];
export declare function emitSequenceDiagram(ir: IRProject): string;
