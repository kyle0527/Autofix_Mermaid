import { IRProject, MermaidFragment } from '@diagrammender/types';
export declare function emitClassDiagramFragments(ir: IRProject): MermaidFragment[];
export declare function emitClassDiagram(ir: IRProject): string;
