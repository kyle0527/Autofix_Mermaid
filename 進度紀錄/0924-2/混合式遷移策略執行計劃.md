# AutoFix Mermaid 混合式遷移策略 - 執行計劃

## 🎯 **策略選擇：混合式漸進遷移**

基於問題演進分析 (90%歷史問題 + 10%新發現) 和架構藍圖，採用**三階段混合策略**：

---

## 🚨 **第一階段：緊急修復 (Week 1-2)**

### **目標**: 恢復核心功能 (30% → 85% 可用性)

#### **Week 1: Tree-sitter 修復**
- [ ] **修復 WASM 載入**
  ```bash
  # 檢查依賴
  npm list tree-sitter tree-sitter-javascript tree-sitter-python
  # 修復載入路徑
  ```
- [ ] **修復 API 調用邏輯**
  ```javascript
  // js/engine/tree-sitter-loader.js:633
  // 修復: 語言檢測 vs 程式碼內容混淆
  async loadLanguage(languageName) { // 不是 codeContent
  ```
- [ ] **建立降級機制**
  ```javascript
  // 確保 Tree-sitter 失敗不影響整體功能
  if (!this.treeSitterAvailable) {
    return this.parseWithRegexFallback(code);
  }
  ```

#### **Week 2: 端到端流程恢復**
- [ ] **Python 解析器修復**
  ```javascript
  // js/engine/python-analyzer.js:111
  // 修復語言物件建立問題
  ```
- [ ] **多語言整合修復**
  ```javascript
  // 加入安全的 metadata 檢查
  const languages = result?.metadata?.languages || ['javascript'];
  ```
- [ ] **端到端測試驗證**
  ```bash
  npm test  # 目標: 31/31 通過
  ```

### **預期成果**: 
- Tree-sitter 功能: 0% → 80%
- 整體測試通過: 96.7% → 100%
- 端到端流程: 失敗 → 成功

---

## 🏗️ **第二階段：架構遷移 (Week 3-8)**

### **目標**: 架構現代化 (85% → 95% 可用性)

#### **Week 3-4: 事件驅動架構**
- [ ] **實作事件總線**
  ```javascript
  // 按照 docs/architecture.md Section 4.1
  // 建立統一事件交換中心
  ```
- [ ] **Pipeline 事件整合** 
  ```javascript
  // 將 MermaidProcessingPipeline 接上事件系統
  pipeline.on('task.started', (event) => eventBus.publish('pipeline.task.started', event));
  ```

#### **Week 5-6: 錯誤隔離完善**
- [ ] **統一錯誤管理**
  ```javascript
  // 擴展 ErrorPropagationManager
  // 建立完整的錯誤恢復策略
  ```
- [ ] **Worker 統一化**
  ```javascript
  // 按照 architecture.md Section 6
  // 逐步淘汰 worker.js，統一到 worker.mjs
  ```

#### **Week 7-8: Pipeline 統一**
- [ ] **PipelineFactory 完善**
  ```javascript
  // 建立 Profile 化管理
  // 同步/異步/批次處理統一入口
  ```
- [ ] **效能優化**
  ```javascript
  // MemoryManager 整合
  // 並行處理能力建立
  ```

### **預期成果**:
- 模組耦合度: 高 → 低
- 錯誤隔離: 不足 → 完善  
- 架構一致性: 分散 → 統一

---

## 🚀 **第三階段：現代化擴展 (Week 9-24)**

### **目標**: 企業級功能 (95% → 100% 完整性)

#### **Month 3: TypeScript 核心**
- [ ] **engine-src 遷移**
  ```typescript
  // 按照 architecture.md Section 7
  // 核心邏輯 TypeScript 化
  ```
- [ ] **型別定義建立**
  ```typescript
  // @diagrammender/types 套件
  // IR, Rule, Pipeline 介面
  ```

#### **Month 4-6: 插件架構**
- [ ] **規則引擎插件化**
- [ ] **語言支援擴展** (Java, C#, Go)
- [ ] **進階圖表類型** (ER, State, Mindmap)

### **預期成果**:
- 語言支援: 2種 → 6種
- 圖表類型: 3種 → 8種
- 企業功能: 基礎 → 完整

---

## 📊 **成功指標與里程碑**

### **Week 2 檢查點**
```bash
✅ npm test 通過率: 31/31 (100%)
✅ Tree-sitter 測試: 6/6 (100%)  
✅ 端到端流程: 成功生成 Mermaid 圖表
✅ 錯誤降級: 優雅 fallback 機制運作
```

### **Week 8 檢查點**
```bash
✅ 事件驅動: 所有模組通過事件通訊
✅ 錯誤隔離: 單一模組失敗不影響整體
✅ Pipeline 統一: PipelineFactory 管理所有處理
✅ Worker 現代化: worker.mjs 成為唯一後端
```

### **Week 24 檢查點**
```bash
✅ TypeScript 核心: engine-src 完全型別化
✅ 插件架構: 規則和語言支援插件化
✅ 企業功能: 多專案、批次處理、監控
✅ 效能目標: 大型專案 < 5分鐘處理
```

---

## 🎯 **立即開始 (今日可執行)**

### **環境檢查**
```bash
cd C:\D\E\Autofix_Mermaid
npm list tree-sitter tree-sitter-javascript tree-sitter-python
find . -name "*.wasm" -type f
```

### **第一個修復**
```javascript
// 目標檔案: js/engine/tree-sitter-loader.js:633
// 問題: Unsupported language: function test() { return 42; }
// 修復: 區分語言名稱與程式碼內容
```

### **測試驗證**
```bash
npm test
# 目標: 解決 Tree-sitter 載入問題
```

---

**策略優勢**: ✅ 低風險 ✅ 快速見效 ✅ 符合架構藍圖 ✅ 業務連續性
**預期時程**: 2週恢復 → 2個月現代化 → 6個月企業級
**成功機率**: 95% (基於現有穩定基礎)