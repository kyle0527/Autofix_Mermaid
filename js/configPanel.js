export function bindConfigPanel(mermaid){const form=document.getElementById('config-form');const cfgEl=document.getElementById('cfg-json');function refresh(){const data=Object.fromEntries(new FormData(form).entries());const cfg={startOnLoad:false,securityLevel:data.securityLevel||'strict',theme:data.theme||'default',flowchart:{curve:data.curve||'basis'}};mermaid.initialize(cfg);cfgEl.textContent=JSON.stringify(cfg,null,2);}form.addEventListener('change',refresh);refresh();}